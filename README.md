# Combinatorial Alice

A modular identity artefact. Turn or swipe the three sections to assemble a temporary Alice.

## Adding a component

1. Put the 800 × 800 PNG in the matching folder under `alice/`.
2. Add its filename and name to the relevant list at the top of `app.js`.

There is no requirement for the three lists to contain the same number of parts.
